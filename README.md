# 🧩 TUI Bots Manager

**TUI Bots Manager** — это консольное (TUI) приложение для управления несколькими Telegram-ботами из одного места.
Проект ориентирован на разработчиков и администраторов, которым важно:
  - централизованное управление ботами;
  - конфигурация через TOML;
  - предсказуемое логирование;
  - минимализм и контроль без веб-панелей 👀

## ✨ Возможности

  - 📦 Управление несколькими Telegram-ботами в одном приложении
  - 🧠 Гибкая конфигурация через config.toml
  - 🔌 Поддержка разных хранилищ (через DSN)
  - 🖥 TUI-интерфейс (ориентирован на клавиатуру)
  - 🪵 Структурированное логирование (slog)
  - 🌱 Поддержка окружений: local, dev, prod

## 🛠 Стек технологий

  - Go >= 1.25.7
  - TOML — конфигурация
  - cleanenv — загрузка и валидация конфига
  - slog / slog-multi — логирование
  - godotenv — переменные окружения (опционально)

## 📦 Зависимости

```go.mod
go 1.25.7

require github.com/ilyakaznacheev/cleanenv v1.5.0
	    github.com/BurntSushi/toml
	    github.com/joho/godotenv
	    github.com/samber/lo
	    github.com/samber/slog-common
	    github.com/samber/slog-multi
	    golang.org/x/text
        gopkg.in/yaml.v3 v3.0.1
	    olympos.io/encoding/edn

```

## ⚙️ Конфигурация

Приложение использует **TOML-конфиг** (`config.toml`).
Пример `config.toml`

```toml 
name = "Bots Manager"
version = "0.0.1"
description = "TUI приложение для управления Telegram-ботами"

[app]
log_level = "dev" # local | dev | prod

[[bots]]
id = "main_bot"
token = "TELEGRAM_BOT_TOKEN"
enable = true

[bots.storage]
driver = "postgres"
dsn = "postgres://user:pass@localhost:5432/bot_db?sslmode=disable"
```

## 🧱 Структура конфига
| Поле | Описание |
| --- | --- |
| name | Название | приложения |
| version |	Версия |
| description	| Описание |
| app.log_level | Уровень логирования |
| bots[].id	| Уникальный ID бота |
| bots[].token | Telegram Bot Token|
| bots[].enable | Включён ли бот |
| bots[].storage | Настройки БД |

## 🪵 Логирование

Используется log/slog с поддержкой нескольких режимов:
| Окружение | Формат | Уровень |
| --- | --- | --- |
| local | text | debug |
| dev | json | debug |
| prod | json |	info (stdout + файл) |

В `prod` логи пишутся:
- в stdout (удобно для Docker)
- в файл log/logs.log

### ▶️ Запуск
```bash 
make run
```
или напрямую:

```bash 
go run ./cmd/app

```
> ⚠️ Перед запуском убедись, что config.toml существует и корректен.

### 🧭 Roadmap
- Полноценный TUI (список ботов, статусы)
- Запуск / остановка ботов из интерфейса
- Просмотр логов по ботам
- Горячая перезагрузка конфигурации
- Поддержка нескольких storage-драйверов

---

📄 Лицензия
MIT (пока не определена окончательно)

---

## 🤝 Автор

Проект разрабатывается в личных целях, но открыт для идей и улучшений.
PR, идеи и обсуждения — приветствуются ☕😄

